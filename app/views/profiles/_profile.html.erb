<div class="profile">
  <ul>
    <% profile.attributes.each do |key, value| %>
      <% next if key == "_id" %>
      <li>
        <%= key.to_s.humanize %> : <%= value %>
      </li>
    <% end %>
  </ul>
</div>